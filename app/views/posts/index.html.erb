<%= @posts.each do |post| %>
    <div class="post">
        <h2 class="post_title">
            <%= post.title %>
        </h2>
        <p class="post_body">
            <%= post.body %>
        </p>
    </div>
<% end %>

<%= link_to "New post", new_post_path %>